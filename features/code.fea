languagesystem DFLT dflt;
languagesystem latn dflt;
languagesystem grek dflt;
languagesystem cyrl dflt;

include (sequences.fea);
include (ligatures.fea);
include (slc-variants.fea);
include (spbase.fea);
include (derani.fea);
include (marks.fea);
